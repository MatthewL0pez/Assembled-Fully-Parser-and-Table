#ifndef EXPRESSION_RESULT_H
#define EXPRESSION_RESULT_H

#include <string>

// Helper struct for Assignment 3 expression checking
// Will help with expressions needed to track their result type
struct ExpressionResult {
    std::string type;
};

#endif 